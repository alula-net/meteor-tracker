extends layout.pug

block content
  h1 Oh no! Something went wrong.

  if production
    p Please try again.
  else
    p= error.message
    if error.isAxiosError
      pre= JSON.stringify(error.toJSON(), null, 2)
    else
      pre= error.stack
